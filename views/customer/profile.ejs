<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title>Customer Profile</title>
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="">
        <link rel="stylesheet" href="/bootstrap.min.css">
        <link  rel="stylesheet" href="/custom_style.css">
    </head>

    
    <body class="background">
        <div  class="card position-absolute top-50 start-50 translate-middle shadow mb-5 bg-white width-auto "  style="margin-top: 30px;">
            <div class="card-content" style="width: 800px;">
                <div class="card-header-blue">
                    <div class="card-header-secondary-text">Mineski Bank - Seychelles </div>
                    <div class="card-header-title">Customer Profile Information</div>
                    </div>
                <div class="card-body">
        <% if (customerExists) {%>
        <!-- individual customer -->
        <% if (privilege_level==4) {%>
        Customer ID: <%= profile[0].customer_id%> <br>
        Full Name: <%= profile[0].full_name%> <br>
        Address: <%= profile[0].address%> <br>
        NIC: <%= profile[0].national_ID%> <br>
        Date of Birth: <%= (JSON.stringify(profile[0].date_of_birth)).toString().substr(1,10)%>
        <br>
        Residential Contact No.:<%= profile[0].residential_contact_no%> <br>
        Personal Contact No.:<%= profile[0].personal_contact_no%> <br>
        Date Joined:<%= (JSON.stringify(profile[0].date_joined)).toString().substr(1,10)%>
        <br>
        Email Address:<%= profile[0].email%> <br>
        <%} else if (privilege_level==3) {%>
        <!-- corporate customer -->
        Company Registration Number: <%= profile[0].company_registration_number%> <br>
        Company Name: <%= profile[0].company_name%> <br>
        Corporate Email Address: <%= profile[0].corporate_email%> <br>
        Address: <%= profile[0].address%> <br>
        Date of Establishment: <%= (JSON.stringify(profile[0].date_of_establishment)).toString().substr(1,10)%>
        <br>
        Contact No.:<%= profile[0].contact_no%> <br>
        Date Joined:<%=  (JSON.stringify(profile[0].date_joined)).toString().substr(1,10)%> <br>
        Correspondent Name:<%= profile[0].correspondent%> <br>
        Correspondent Email Address:<%= profile[0].correspondent_email%> <br>
        <%}%>
                    
        <%} else{%>
        This customer does not exist. Create a customer profile before creating accounts.
        <br>
        <form action="/employee/customer/individual" method="GET">
            <input type="submit" value="Create an Individual Customer Profile">
        </form>
        <br>
        <%}%>
            <br>
            <form action="/" method="GET">
                <input type="submit" value="Home" class="button" style="width: 200px; background-color:#ff1744;">
            </form>
                </div></div></div>  
    </body>
</html>