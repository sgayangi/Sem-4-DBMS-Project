<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title></title>
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="/bootstrap.min.css">
        <link  rel="stylesheet" href="/custom_style.css">
    </head>
    <body class="background">
        <div class="card position-absolute top-50 start-50 translate-middle shadow mb-5 bg-white width-auto"
            style="margin-top: 20px;">
            <div class="card-content" style="width: 1000px;">
                <div class="card-header-blue">
                    <div class="card-header-secondary-text">Mineski Bank - Seychelles </div>
                    <div class="card-header-title">Installments Report</div>
                    <div class="card-header-secondary-text">
                        <%if(branch ==1){%>
                            Head Office
                        <% } else{%> 

                            Branch ID: <%= branch %> 
                        <% } %> 
                         </div>

                    </div>
                <div class="card-body" style="height:400px; overflow-y:auto;">
        <% const months = ['January','February','March','April','May','June','July','August','September','October','November','December']%>
        <h2 style="color: #113F86;">Late Loan Report Installments for <%= months[parseInt(month) -1] %>
            of <%=year%></h2>
        <% if (installments.length>0) {%>
            
        <table class="table table-bordered">
            <thead>
                <th>Installment ID</th>
                <%if(branch ==1){%>
                    <th>Branch ID</th>
                <% } %> 
                <th>Loan ID</th>
                <th>Installment </th>
                <th>Remaining Installment</th>
                <th>Status</th>

            </thead>
            <tbody>
                <% for(var i=0; i<installments.length;i++) {%>
                <tr>
                    <td><%= installments[i].installment_id%></td>
                    <%if(branch ==1){%>
                        <td> <%= installments[i].branch_id%> </td>
                    <% } %> 
                    <td><%= installments[i].loan_id%></td>
                    <td><%= installments[i].loan_installment%></td>
                    <td><%= installments[i].remaining_no_of_installments%></td>
                    <td><%= installments[i].status%></td>
                </tr>
                <%}%>
                </tbody>
            </table>
                <%}else{%>
                    <div style="color:#ff1744; font-size: x-large; font-weight: 500;" > No Late Loan installments for the selected month </div>
                    <% } %> 

                <br>
                </div>
                <div class="container" style="margin-bottom: 10px;">
                    <form action="/" method="GET">
                        <input type="submit" value="Home" class="button"
                            style="background-color: #ff1744; width:200px;">
                    </form>
                </div>

                </div>
                </div>
    </body>
</html>