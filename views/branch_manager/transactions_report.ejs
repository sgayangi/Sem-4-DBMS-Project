<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title></title>
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="/bootstrap.min.css">
        <link  rel="stylesheet" href="/custom_style.css">
    </head>
    <body class="background">
        <div class="card position-absolute top-50 start-50 translate-middle shadow mb-5 bg-white width-auto"
            style="margin-top: 20px;">
            <div class="card-content" style="width: 1000px;">
                <div class="card-header-blue">
                    <div class="card-header-secondary-text">Mineski Bank - Seychelles </div>
                    <div class="card-header-title">Transactions</div>
                    <div class="card-header-secondary-text">
                        <% if(branch ==1) {%> 
                             Head Office
                        <% }else { %> 
                            Branch ID: <%= branch %>
                         <%}%>      
                         </div>
                    </div>
                <div class="card-body"  style="height:400px; overflow-y:auto;">

        <% if (transactions.length>0) {%>
        <table class="table table-bordered">
            <thead>
                <% if(branch ==1) {%>  
                <th>Branch ID</th>
                <% } %> 
                <th>Transaction ID</th>
                <th>Date</th>
                <th>Initiating Account</th>
                <th>Receiving Account</th>
                <th>Transaction Amount (Rs.)</th>

            </thead>
            <tbody>
                <% for(var i=0; i<transactions.length;i++) {%>
                <tr>
                    <% if( branch ==1) {%>  
                        <td><%= transactions[i].branch_id%></td>
                        <% } %> 
                    <td><%= transactions[i].transaction_id%></td>
                    <td><%=(JSON.stringify(transactions[i].date)).toString().substr(1,10) %></td>
                    <td><%= transactions[i].initiating_account_id%></td>
                    <td><%= transactions[i].receiving_account_id%></td>
                    <td><%= transactions[i].transaction_amount%></td>
                </tr>
                <%}%>
                </tbody>
            </table>
                <%} else{%>
                    <div style="color:#ff1744; font-size: x-large; font-weight: 500;" > No transactions for the selected month </div>
                    <% } %> 
        <br>
        </div>
        <div class="container" style="margin-bottom: 10px;">
            <form action="/" method="GET">
                <input type="submit" value="Home" class="button" style="background-color: #ff1744; width:200px;">
            </form>
        </div>

        </div>
        </div>
    </body>
</html>